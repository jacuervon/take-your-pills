from storage import retrieve_data, store_data, get_user_folder, set_path
from crypto import generate_key
DATA_FILE_NAME = "data_take_pill.json"


def main():
    
    # path_json_file = set_path(get_user_folder(), DATA_FILE_NAME);
    # data = retrieve_data(path_json_file)
    # data["test"] = ["test1", "test2"]
    # store_data(DATA_FILE_NAME, data)
    # print(data)
    pass


if __name__ == '__main__':
    main()
